{
  "name": "dust-outpost",
  "version": "1.0.0",
  "description": "CS-style browser FPS — team deathmatch on a Dust2-inspired map",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "5.9.3",
    "vite": "^6.2.0",
    "vitest": "^3.0.9"
  }
}
